import {useCallback,useState} from 'react';import {QRCodeCanvas} from 'qrcode.react';import {buscarEstudiantePorDni} from '../services/api';
export default function Generator(){const [dni,setDni]=useState('');const [result,setResult]=useState(null);const [loading,setLoading]=useState(false);const generate=useCallback(async()=>{if(!/^\d{8}$/.test(dni)){setResult({success:false,message:'El DNI debe contener 8 dígitos.'});return}setLoading(true);setResult(null);try{const data=await buscarEstudiantePorDni(dni);setResult(data)}catch(e){setResult({success:false,message:e.message})}finally{setLoading(false)}},[dni]);return <main className="mx-auto max-w-6xl px-4 py-10"><div className="mx-auto max-w-xl text-center"><h1 className="mt-4 text-3xl font-black text-white md:text-4xl">Generador de QR</h1><p className="mt-2 text-slate-400">Introduce el DNI del estudiante para generar su código QR de asistencia.</p></div><div className="mx-auto mt-8 max-w-lg rounded-3xl border border-blue-900/50 bg-slate-900/50 p-5 shadow-xl shadow-blue-950/5 backdrop-blur-sm"><div className="flex flex-col gap-4 sm:flex-row"><input value={dni} onChange={e=>setDni(e.target.value)} type="search" placeholder="Introduce el DNI de 8 dígitos..." className="w-full rounded-xl border-0 bg-slate-800/50 px-4 py-3 text-white placeholder-slate-400 ring-1 ring-inset ring-slate-700/50 focus:ring-2 focus:ring-blue-600"/><button onClick={generate} disabled={loading} className="rounded-xl bg-blue-600 px-5 py-3 text-sm font-semibold text-white enabled:hover:bg-blue-500 disabled:opacity-50">{loading?'Verificando...':'Generar QR'}</button></div> {loading&&<div className="grid min-h-40 place-items-center"><div className="text-center"><div className="mx-auto h-10 w-10 animate-spin rounded-full border-4 border-slate-700 border-t-blue-600"/><p className="mt-4 font-semibold">Buscando estudiante...</p></div></div>} {result&&<div className={`mt-5 rounded-2xl p-5 ${result.success?'bg-blue-600/20':'bg-red-500/20'}`}><h2 className="text-center font-bold">{result.message}</h2>{result.success&&result.estudiante&&<div className="mt-4 flex flex-col items-center gap-4 text-center"><p className="font-bold">{result.estudiante.nombre}</p><div className="rounded-xl bg-white p-4"><QRCodeCanvas value={result.estudiante.dni} size={160}/></div><p className="text-sm text-slate-300">Escanea este código para registrar la asistencia.</p></div>}</div>}</div></main>}