import {useCallback,useState} from 'react';import QRScanner from '../components/QRScanner';import {registrarAsistencia} from '../services/api';
export default function Scanner(){const [result,setResult]=useState(null);const [loading,setLoading]=useState(false);const process=useCallback(async dni=>{if(!dni){setResult({success:false,message:'No se pudo acceder a la cámara.'});return}setLoading(true);setResult(null);try{setResult(await registrarAsistencia(dni))}catch(e){setResult({success:false,message:e.message})}finally{setLoading(false)}},[]);return <main className="mx-auto max-w-6xl px-4 py-10"><div className="mx-auto max-w-xl text-center"><span className="rounded-full bg-blue-100 px-3 py-1 text-xs font-bold uppercase tracking-wider text-blue-900">Registro en tiempo real</span><h1 className="mt-4 text-3xl font-black text-slate-900 md:text-4xl">Escanear asistencia</h1><p className="mt-2 text-slate-500">El QR debe contener únicamente el DNI del estudiante.</p></div><div className="mx-auto mt-8 max-w-lg rounded-3xl border border-blue-100 bg-white p-5 shadow-xl shadow-blue-950/5">{loading?<div className="grid min-h-80 place-items-center"><div className="text-center"><div className="mx-auto h-10 w-10 animate-spin rounded-full border-4 border-slate-200 border-t-blue-800"/><p className="mt-4 font-semibold">Registrando asistencia...</p></div></div>:<QRScanner onScan={process}/>} {result&&<div className={`mt-5 rounded-2xl p-5 ${result.success?'bg-blue-50 text-blue-950':'bg-red-50 text-red-900'}`}><div className="text-center text-3xl">{result.success?'✓':'!'}</div><h2 className="mt-2 text-center font-bold">{result.message}</h2>{result.estudiante&&<div className="mt-4 text-center text-sm"><p className="font-bold">{result.estudiante.nombre}</p><p>{result.estudiante.grado} · {result.estudiante.nivel}</p>{result.asistencia&&<p className="mt-2">{result.asistencia.fecha} · {result.asistencia.hora} · <b>{result.asistencia.estado}</b></p>}</div>}</div>}</div></main>}