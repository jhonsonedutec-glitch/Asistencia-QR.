import {useEffect,useState} from 'react';import {obtenerDashboard} from '../services/api';import {ResponsiveContainer,BarChart,Bar,XAxis,YAxis,Tooltip} from 'recharts';
export default function Dashboard(){const [months,setMonths]=useState(1);const [data,setData]=useState(null);const [error,setError]=useState('');useEffect(()=>{obtenerDashboard(months).then(setData).catch(e=>setError(e.message))},[months]);const m=data?.metricas||{};const chart=[{name:'Asistió',total:m.asistencias||0},{name:'Tardanza',total:m.tardanzas||0}];return <main className="mx-auto max-w-6xl px-4 py-10"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><span className="text-xs font-bold uppercase tracking-widest text-emerald-700">Dirección</span><h1 className="mt-1 text-3xl font-black text-slate-900">Dashboard de asistencia</h1><p className="mt-1 text-slate-500">Resumen de registros del periodo seleccionado.</p></div><div className="flex rounded-2xl bg-white p-1 shadow-sm ring-1 ring-slate-200">{[1,3,6].map(n=><button key={n} onClick={()=>setMonths(n)} className={`rounded-xl px-4 py-2 text-sm font-bold ${months===n?'bg-emerald-900 text-white':'text-slate-600'}`}>{n} mes{n>1?'es':''}</button>)}</div></div>{error&&<div className="mt-6 rounded-2xl bg-red-50 p-4 text-red-800">{error}</div>}<section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Registros',m.total||0],['Asistencias',m.asistencias||0],['Tardanzas',m.tardanzas||0],[ 'Asistencia',`${m.porcentaje||0}%`]].map(([label,value])=><div key={label} className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-black text-emerald-900">{value}</p></div>)}</section><section className="mt-6 grid gap-6 lg:grid-cols-2"><div className="rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="mb-4 font-bold">Comparativo</h2><ResponsiveContainer width="100%" height={260}><BarChart data={chart}><XAxis dataKey="name"/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="total"/></BarChart></ResponsiveContainer></div><div className="overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-slate-200"><div className="border-b p-5"><h2 className="font-bold">Registros recientes</h2></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="px-4 py-3">Fecha</th><th className="px-4 py-3">Hora</th><th className="px-4 py-3">Estudiante</th><th className="px-4 py-3">Estado</th></tr></thead><tbody>{(data?.registros||[]).slice(0,10).map((r,i)=><tr key={i} className="border-t"><td className="px-4 py-3">{r.fecha}</td><td className="px-4 py-3">{r.hora}</td><td className="px-4 py-3 font-medium">{r.nombre}</td><td className="px-4 py-3">{r.estado}</td></tr>)}</tbody></table></div></div></section></main>}
